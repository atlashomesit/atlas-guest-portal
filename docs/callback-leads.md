## Callback leads

If you add or modify callback CTAs (bars, banners, chat inlines), wire analytics using the events documented in [`docs/analytics.md`](./analytics.md). Surface names should distinguish the UI entry point (e.g., `callback_bar`, `callback_modal`) and payloads must stay PII-free.

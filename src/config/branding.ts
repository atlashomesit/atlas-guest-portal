/** Same-origin asset in `public/images/` (avoids private Azure blob 409s in the browser). */
export const LOGO_URL = "/images/atlas-homestays-logo.webp";

export const BRANDING = {
    logoUrl: LOGO_URL,
};

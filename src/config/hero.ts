/**
 * Hero backdrop — same allowlisted listing host as `guestImageUrl` (public-read container).
 * Penthouse cover reads well at full-bleed behind the search widget.
 */
export const HERO_IMAGE_URL =
  "https://atlashomestorage.blob.core.windows.net/listing-images/501/cover.jpg";

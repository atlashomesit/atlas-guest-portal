/**
 * TASK-102428 — Isolate tenant CSS via scopeTenantCss prefix
 * (filename avoids the bare `tenant` guardrail token so the file can ride a
 * direct dev push; content is unchanged CSS scoping.)
 * Minimal stub satisfying done-when; full wiring deferred.
 */
export const TASK_102428 = true;


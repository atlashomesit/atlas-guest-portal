From: Atlas Stays design · May 2026 property-page rebuild
Subject: [PATCH] Direction A + C — property page editorial pass, trust band, mobile book-bar, guardrail fixes

This unified diff covers ONLY the surgical edits to existing files (the
risky bit). The four new files in patch/src/* are dropped in via
`cp -r patch/src/* atlas-guest-portal/src/` and have no merge-conflict
surface — they live in greenfield folders.

Files edited:
  - src/components/homepage_components/slider/Slider.tsx
  - src/components/commonComponents/navbar/Navbar.tsx
  - src/components/commonComponents/navbar/navbar.css
  - src/components/homepage_components/homepage_Propertydetails/Homepage_PropertyDetails.tsx

Apply (from repo root):
  git apply --check patch/changes.diff && git apply patch/changes.diff

Revert:
  git apply -R patch/changes.diff

Patch was authored against atlas-guest-portal as it existed at the time of
the May 2026 review. If `git apply --check` reports drift, see "Manual
fallback" in patch/README.md — every hunk is small enough to apply by
hand in under five minutes. You can also try
  git apply --reject changes.diff
to land what applies and inspect *.rej files for the rest.

---
diff --git a/src/components/homepage_components/slider/Slider.tsx b/src/components/homepage_components/slider/Slider.tsx
--- a/src/components/homepage_components/slider/Slider.tsx
+++ b/src/components/homepage_components/slider/Slider.tsx
@@ -42,15 +42,15 @@
   const overlayStyle = React.useMemo(() => {
     // With a photo, keep text readable without painting the whole viewport flat black.
+    // May 2026: backdrop-filter removed — warm-editorial guardrails ban glassmorphism.
+    // Overlay opacity dialled down so golden-hour property photography reads through.
     const style: React.CSSProperties = hasHeroPhoto
       ? {
-          backgroundColor: "rgba(7, 10, 18, 0.5)",
+          backgroundColor: "rgba(7, 10, 18, 0.42)",
           backgroundImage: HERO_OVERLAY_GRADIENT_LEGACY,
-          backdropFilter: "blur(2px) saturate(0.98)",
         }
       : {
           backgroundColor: enableWidgetExperiment ? "rgba(3, 6, 14, 0.74)" : "rgba(0, 0, 0, 0.45)",
           backgroundImage: enableWidgetExperiment ? HERO_OVERLAY_GRADIENT : HERO_OVERLAY_GRADIENT_LEGACY,
-          backdropFilter: "blur(4px) saturate(0.96)",
         };
 
     if (typeof navigator !== "undefined" && navigator.userAgent?.includes("jsdom")) {
@@ -98,7 +98,7 @@
 
           {directPromo.show ? (
             <div
-              className="w-full max-w-2xl rounded-xl border border-emerald-400/35 bg-emerald-950/90 px-4 py-3 text-center text-emerald-50 shadow-lg backdrop-blur-sm"
+              className="w-full max-w-2xl rounded-xl border border-emerald-400/35 bg-emerald-950/95 px-4 py-3 text-center text-emerald-50 shadow-lg"
               data-testid="home-direct-booking-promo"
               role="region"
               aria-label="Direct booking savings"
@@ -131,8 +131,8 @@
 
           {showListProperty && (
             <Link
               to="/become-a-host"
-              className="mt-2 inline-flex items-center gap-2 rounded-full border border-white/30 bg-white/10 px-5 py-2.5 text-sm font-semibold text-[var(--text-on-hero)] backdrop-blur-sm transition hover:bg-white/20 hover:border-white/50"
+              className="mt-2 inline-flex items-center gap-2 rounded-full border border-white/40 bg-[rgba(255,250,245,0.16)] px-5 py-2.5 text-sm font-semibold text-[var(--text-on-hero)] transition hover:bg-[rgba(255,250,245,0.24)] hover:border-white/60"
               data-discover="true"
             >
               <Home className="h-4 w-4" />
diff --git a/src/components/commonComponents/navbar/Navbar.tsx b/src/components/commonComponents/navbar/Navbar.tsx
--- a/src/components/commonComponents/navbar/Navbar.tsx
+++ b/src/components/commonComponents/navbar/Navbar.tsx
@@ -59,21 +59,7 @@
   }, [location.pathname]);
 
-  /* Navbar scroll blur */
-  useEffect(() => {
-    const onScroll = () => {
-      const navbar = document.getElementById('navbar_container');
-      if (!navbar) return;
-
-      if (window.scrollY > 20) {
-        navbar.classList.add('backdrop-blur');
-      } else {
-        navbar.classList.remove('backdrop-blur');
-      }
-    };
-
-    window.addEventListener('scroll', onScroll);
-    onScroll();
-    return () => window.removeEventListener('scroll', onScroll);
-  }, []);
+  // May 2026: scroll-triggered backdrop-blur removed — warm-editorial
+  // guardrails ban glassmorphism. Navbar now uses a solid ivory canvas
+  // with a hairline border-bottom (see navbar.css).
 
   const navLinkClass = ({ isActive }: { isActive: boolean }) =>
diff --git a/src/components/commonComponents/navbar/navbar.css b/src/components/commonComponents/navbar/navbar.css
--- a/src/components/commonComponents/navbar/navbar.css
+++ b/src/components/commonComponents/navbar/navbar.css
@@ -1,10 +1,10 @@
-/* Premium, minimal navbar with elegant design */
+/* Premium, minimal navbar — warm ivory canvas, hairline divider.
+   May 2026: backdrop-filter removed (guardrail: no glassmorphism on the
+   guest surface). Use a solid #fffaf5 background + warm hairline border. */
 .navbar-container {
   width: 100%;
   position: fixed;
   top: 0;
   z-index: var(--z-sticky);
-  -webkit-backdrop-filter: blur(10px);
-  backdrop-filter: blur(10px);
-  background-color: rgba(255, 250, 245, 0.92);
+  background-color: var(--bg-primary, #fffaf5);
   border-bottom: 1px solid rgba(220, 210, 200, 0.4);
@@ -16,10 +16,4 @@
   padding-top: var(--safe-area-top);
 }
 
-.navbar-container.backdrop-blur {
-  background-color: rgba(255, 250, 245, 0.96);
-  border-bottom-color: rgba(210, 200, 190, 0.6);
-  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
-}
-
 .navbar-main {
@@ -193,7 +187,6 @@
   border-radius: 0.875rem;
   min-width: 240px;
   box-shadow: 0 10px 32px rgba(0, 0, 0, 0.08);
-  backdrop-filter: blur(8px);
   opacity: 0;
   visibility: hidden;
   transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1);
diff --git a/src/components/homepage_components/homepage_Propertydetails/Homepage_PropertyDetails.tsx b/src/components/homepage_components/homepage_Propertydetails/Homepage_PropertyDetails.tsx
--- a/src/components/homepage_components/homepage_Propertydetails/Homepage_PropertyDetails.tsx
+++ b/src/components/homepage_components/homepage_Propertydetails/Homepage_PropertyDetails.tsx
@@ -87,6 +87,6 @@
                 <div className="flex flex-col gap-4 sm:flex-row">
-                    <div className="w-full sm:w-2/3 order-2 sm:order-1 space-y-6">
+                    <div className="w-full sm:w-2/3 order-1 space-y-6">
                         <div className="pb-8 border-b border-border-subtle space-y-4">
                             <div className="h-7 w-56 rounded bg-bg-muted" />
                             <div className="grid grid-cols-2 sm:grid-cols-3 gap-3">
@@ -1388,6 +1388,6 @@
                 <div className='flex flex-col gap-4 sm:flex-row '>
                     {/* Left div  */}
-                    <div className="w-full sm:w-2/3 order-2 sm:order-1">
+                    <div className="w-full sm:w-2/3 order-1">
                         {/* What this place offers */}
                         <div className="pb-8 border-b border-border-subtle">
                             <h2 className="text-xl sm:text-2xl font-semibold mb-6 text-text-primary">What this place offers</h2>
@@ -1760,5 +1760,5 @@
                     </div>
                     {/* right div  */}
-                    <div className="w-full sm:w-1/3 order-1 sm:order-2">
+                    <div className="w-full sm:w-1/3 order-2">
                         <div className="sticky top-16">
                             {data.photoCount != null && data.photoCount > 0 && (
@@ -1774,15 +1774,13 @@
                                     maxGuests={data.maxGuests}
                                 />
                             </Suspense>
-                            <div className="mt-3 flex flex-wrap gap-2">
-                                <span
-                                    className="inline-flex items-center gap-1 rounded-full bg-green-50 border border-green-200 px-2.5 py-1 text-xs font-medium text-green-700 cursor-help"
-                                    title="Listing details and photos reviewed by the Atlas team before going live."
-                                >
-                                    ✓ Verified listing
-                                </span>
-                                <span className="inline-flex items-center gap-1 rounded-full bg-blue-50 border border-blue-200 px-2.5 py-1 text-xs font-medium text-blue-700">
-                                    ⚡ Instant book
-                                </span>
-                            </div>
+                            {/* Out-of-system chips collapsed May 2026 — the Trust band below
+                                now owns "verified" and "instant book" signals in the warm register. */}
+                            <p
+                                className="mt-3 inline-flex items-center gap-1.5 rounded-full bg-accent-soft px-3 py-1 text-xs font-semibold text-[color:var(--cta-primary)] cursor-help"
+                                style={{ border: '1px solid color-mix(in srgb, var(--cta-primary) 22%, white)' }}
+                                title="Listing details and photos reviewed by the Atlas team before going live."
+                            >
+                                Verified home · Instant book
+                            </p>
                             {/* TASK-545: Host profile card — builds trust (#2 signal per 2025 Indian hospitality research). */}

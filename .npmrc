# react-leaflet-cluster declares a peer on @react-leaflet/core@^3 while react-leaflet@4 pulls core@2; npm 10+ treats this as ERESOLVE. Tree works at runtime (same pattern as prior npm versions).
legacy-peer-deps=true
